---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: tautulli-julianmaze-com
spec:
  hostnames:
    - tautulli.julianmaze.com
  parentRefs:
    - name: default-gateway
      namespace: gateway
      sectionName: tautulli-legacy-passthrough # name of the gateway listener
  rules:
    - name: tautulli-julianmaze-com
      backendRefs:
        - name: jmaze-k8s-default-ingress
          port: 443
          kind: Service
