apiVersion: v1
kind: Secret
metadata:
  name: dhi-registry
  namespace: sbi
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: changeme-base64-encoded-docker-config
# # Login to dhi.io
# docker login dhi.io

# # Create the secret from your Docker credentials
# kubectl create secret generic dhi-registry \
#   --from-file=.dockerconfigjson=$HOME/.docker/config.json \
#   --type=kubernetes.io/dockerconfigjson \
#   --namespace=sbi \
#   --dry-run=client -o yaml > registry-secret.yaml
